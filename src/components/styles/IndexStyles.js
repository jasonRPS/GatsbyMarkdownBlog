import styled from "styled-components"

export const IndexStyles = styled.div`
  #carousel {
    position: relative;
    background: gray;
    height: 600px;
    overflow: hidden;
  }

  #call-to-action {
    display: flex;
    justify-content: space-evenly;
  }
`
